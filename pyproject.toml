[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "stepmix"
authors = [{name = "Sacha Morin", email = "sacha.morin@mila.quebec"}, {name = "Robin Legault", email = "robin.legault@umontreal.ca"}]
dynamic = ["version", "description"]
